# CLAUDE.md — Fitness Coaching App

You are building a coach-branded fitness app: iOS + Android (Expo React Native) + website/admin (Next.js), backend on Neon Postgres. Full spec lives in PROJECT_PLAN.md — read it before any architectural decision.

## Stack
- Turborepo + pnpm. apps/mobile (Expo SDK 57, expo-router, TypeScript strict, StyleSheet + tokens), apps/web (Next.js App Router), packages/shared (types + zod), packages/ui-tokens, packages/db (Drizzle + Neon).
- **Neon Postgres** (decided 2026-07-03, replaces Supabase): schema in packages/db via Drizzle, mobile talks to it only through the API layer (apps/web routes), never directly. Auth provider TBD. RevenueCat for subscriptions. Zustand for state. expo-sqlite offline-first with a sync queue.
- Styling deviation (decided 2026-07-03): plain StyleSheet + @gym/ui-tokens instead of NativeWind — NativeWind v4 pins Tailwind v3 and has a history of breaking on SDK upgrades; v5 is pre-release. Zero-dependency tokens are upgrade-proof and work identically on web.

## Hard rules
1. **TypeScript strict. No `any`.** Shared types live in packages/shared only.
2. **Feature modules are isolated.** features/X never imports from features/Y — only from packages/* and lib/*.
3. **Every new table ships with RLS policies in the same migration.** Owner-only by default.
4. **Tier gating** only via `hasEntitlement(user, feature)` from packages/shared. Never hardcode tier names in screens.
5. **Offline-first**: log writes hit SQLite first, sync queue second. UI must confirm in <100ms.
6. **Accessibility is not optional**: touch targets ≥48dp, body text ≥16px, contrast ≥4.5:1, labels on all icons, max 2 primary actions per screen. Respect the user's font-scale setting.
7. Design tokens from packages/ui-tokens only. No inline hex colors in screens.
8. Zod-validate every payload crossing the network boundary.
9. Secrets in .env / EAS secrets. Never commit keys.
10. Write tests for: PR detection, weight trend smoothing, macro math, sync-queue conflict handling, entitlement checks.

## Commands
- `pnpm dev` — web · `npx expo start` (in apps/mobile) — mobile
- `pnpm typecheck && pnpm lint && pnpm test` — run before every commit
- `pnpm supabase:migrate` — apply migrations locally

## Conventions
- Commits: conventional (feat:, fix:, chore:). One feature module per branch.
- Screens = thin; logic in feature `logic.ts` (testable, pure where possible).
- Naming: plain language user-facing ("Food", "Train", "Buddy").

## Progress log (update at end of each session)
- [x] Scaffold monorepo (2026-07-03)
- [x] Neon Postgres schema via Drizzle in packages/db (replaces Supabase; RLS N/A — access goes through the API layer)
- [x] ui-tokens + base components (charcoal/red reference design, Poppins+Oswald, motion vocabulary in components/ui/motion.ts)
- [~] Auth: email/password vs Neon via apps/web API + optional sign-in on mobile; Google↔password account linking (password-proven, /api/auth/google + GoogleLinkPrompt); post-login/onboarding nav resets the whole stack via lib/nav resetStackTo (back can't reopen Welcome) (Google/Apple pending OAuth credentials)
- [x] Onboarding quiz + targets (12-step wizard incl. the 2026-07-12 "stay on track" permission step, computeTargets)
- [x] Training: plans, logger, gym mode, rest timer, plate calculator, true-3D Z-Anatomy selector with MuscleMapJS/SVG fallback, PR detection (unit-tested)
- [x] True-3D anatomy (2026-07-11): shared offline Three.js WebView/iframe viewer; clean Z-Anatomy outer body, neutral pelvis closure, 17 red/orange heat-map highlights, tap/orbit/zoom/front-back controls, SVG runtime fallback, and CC BY-SA attribution
- [x] Body: weight/trend (EWMA smoothing), measurements (photos pending)
- [x] Nutrition: kcal tracker, Open Food Facts + USDA search, barcode scan, water, custom foods
- [~] Subscription: GM Method tier catalog + paywall + hasEntitlement gating (RevenueCat pending store accounts)
- [~] Engagement: streaks + PR moment done; share cards + push pending
- [ ] Buddy Sync (Phase 2)
- [ ] Admin dashboard (Phase 2)
- [ ] Sync queue: device SQLite ↔ Neon via API (architecture ready — repo layer + accounts exist)
- [x] Coach-Trainee Mentorship (2026-07-10): discovery hub (/coaches + /coaches/[id]), member requests + coach accept/decline (coach_requests table, one pending per member, capacity-gated), coach portfolio (coachProfiles + headline/specialties/certifications/achievements/years/capacity; edited in staff/coach/profile), coach-logged client milestones (coach_milestones → member Progress portfolio), server-side PII masking on ALL coach-member text (maskPii in @gym/shared, unit-tested), chat unlocked by active assignment OR elite, coach identity data-driven (no more hardcoded Greece), client emails stripped from coach console. Full spec: docs/mentorship-system.md. New tables land on next `pnpm db:push`.
- [x] 2026-07-10 hardening pass: fluid sliding-disc FloatingTabBar (squash-stretch, live-workout dot, reduce-motion); initial pseudo-3D anatomy explorer (superseded by the 2026-07-11 true-3D viewer above); muscle-map tables hoisted to lib/muscleMap.ts; food quality (Nutri-Score/NOVA/fiber/sugar/sodium) from OFF+USDA end-to-end; BILLING_MODE=live gate + RevenueCat webhook (/api/subscription/revenuecat) — paid tiers can't be self-granted in live mode; tier gating unified on useEffectiveTier (local tier = signed-out preview only; trial no longer writes it); profile cloud-restore claim-lock; ~45 audited fixes (sheets/keyboard containment, touch targets, spacing rhythm, duplicate PR stamps, measurement units, silent failures, buddy double-taps, backend indexes/rate limits/enumeration oracle — indexes land on next `pnpm db:push`)
- [x] 2026-07-17/18 buddy removal + home/coach/visual overhaul: Buddy feature deleted end-to-end (4-tab bar; referrals live on as Settings→"Invite friends" + /invite screen w/ already_enrolled 409; public leaderboard + trials + coach challenge relocated to non-buddy modules); Home gained Progress-report/Weight/Enrollment cards; public coach profile redesigned (gradient hero, tier badge, milestones) + coach photoUrl upload end-to-end (Cloudinary); photo-hero rollout (shared PhotoHero, 10+ workout-keyed stock photos via photoForWorkout, hero glow motifs); demo coach seed (packages/db seed:demo-coach).
- [x] 2026-07-17/18 admin program (audit→P0→P1/P2, ~280 agents): 140-agent audit (118 confirmed defects, all fixed); RBAC in packages/shared/src/logic/permissions.ts (fail-closed hasPermission, role presets, per-account admin_permission_overrides merged in authz.ts, GRANTABLE_ROLES excludes nutrition_admin); money hardening (extend-vs-overwrite tier windows, idempotent CAS approvals, refunds w/ rollback+clawback, RevenueCat no-clobber via accounts.tier_source); new surfaces: broadcast push, ops-queue overview, coach offboarding cascade w/ dry-run, coach payouts (requests+admin queue+web coach wallet), analytics, moderation queues, CSV exports, support lifecycle (open/resolved/assignee), member lifecycle tools (reset links, force sign-out, GDPR anonymize), staff re-auth (/api/staff/reauth + ReauthGate); mobile admin parity (pricing/wallets/tier-requests/payouts screens); schema pushed to Neon.
- [x] 2026-07-18 marketplace mega-upgrade (research→12-package build, plan in session scratchpad mega-plan.md): meal delivery vertical (meal_partners/meals/orders+items+events/subscriptions/billing cycles/meal_payment_requests/delivery config; 7-state order machine + KTM UTC+05:45 cutoffs in @gym/shared meals.ts/orders.ts; materialize-on-read subscription orders via partial-unique CAS; prepaid digital cycles, COD reconciles on delivery; member flows at /meals* + Meals tab); isolated restaurant partner portal (role 'partner' rank-0, requirePartner scoped to own restaurant, /partner web portal, PII-minimal order projection, excluded from admin/coach consoles + override targets); Nearby Gyms vertical (gyms+gym_photos schema, admin CRUD w/ photo upload, public /api/gyms, Gyms tab + /gyms/[slug], wave-health-club-ktm draft seed); admin console redesigned to light-SaaS shell (globals.css tokens + components/console/* — SidebarNav/TopBar/ChartCard/GaugeArc/HeatGrid pure SVG, all pages converted, coach+partner layouts on same shell); tab bar now 6 tabs (Home/Train/Food/Meals/Gyms/Progress) w/ responsive item width + safe-area insets; web-platform motion disabled in motion.ts (Reanimated entering broke web paint). All gates green: web+mobile tsc 0 errors, packages/shared 430/430, db push applied, findex reindexed. NOT committed — working tree holds ~147 changed/new files.
- [x] 2026-07-12 scale-up (docs/SCALE-UP-PLAN.md W1-W5): buddy-session join + participant-visibility + referral-discount bugs fixed, onboarding "Stay on track" permission step (no-prompt notification/step-permission split); self-serve coach enrollment (coach_applications) + admin verify/reject + coach seniority tiers (silver/gold/elite, tier-request flow); promo codes (auto per verified coach, 30% off/30% commission + admin house codes) + coach wallet ledger + regional pricing catalog (NP NPR / INTL USD, admin pricing editor); manual Nepal payment requests (eSewa/Khalti receipt upload → admin approve/reject → dated tier grant); coach-assigned workouts + diet plans for silver+/gold+ clients; admin support inbox (threaded, unread-driven); friend-to-friend DMs on buddy links; image uploads (avatars, receipts, progress photos) via Cloudinary signed/authenticated delivery. Full spec + remaining deferred items: docs/SCALE-UP-PLAN.md.
- [x] 2026-07-19 card studio + Android-nav hardening (v1.1.0/v1.1.1): membership-card design system — 10 selectable faces (Brushed/Guilloché/Monogram/Art Deco/Carbon/Marble/Blueprint/Holographic/Minimal/Racing) in features/subscription/components, shared CardFaceProps contract + registry (cardDesigns.ts), persisted picker (state/cardDesign.ts + /membership-card-design; entry = palette badge riding the Settings card), MembershipCardAny dispatcher in Settings + /membership-card; elite pass (10 agents): warm-gold inkDim re-ink for visibility on the near-black elite metal + one elite-exclusive flourish per face, other tiers untouched; /membership-card reframed from gym check-in to partner-restaurant member discount (names the member's meal-order partner) + member code grouped in fours (no more overflow); web /api CORS for Expo-web dev origins (src/middleware.ts — live locally, hosted API needs a deploy); Android 3-button nav overlap fixed app-wide via useBottomClearance (lib/systemBars, 48dp fallback when OEM builds report insets.bottom=0 under edge-to-edge) — workout dock + FloatingTabBar inline, then a 14-agent sweep migrated Screen, Sheet, GymActionBar, coach CTA/thread composer, meal cart bar, scan sheet, exercises list, admin tier sheet, welcome CTA, coach roster (v1.1.1). All gates green: mobile+web tsc 0 errors, shared 508/508.
- [x] 2026-07-21 marketing-site revamp "Iron & Ember": full customer site rebuilt from scratch on Tailwind v4 (deps: tailwindcss + @tailwindcss/postcss + lucide-react; utilities scoped under `.mkt` in src/app/marketing.css so consoles keep globals.css — note: unlayered globals h1-h4 rules are counter-overridden there) — 13 pages (home/training/nutrition/progress/coaching/meals/gyms/pricing/partners/for-coaches/download/about + contact/legal restyle), design system in components/marketing (SPEC.md = law: aurora light fields, glass cards, gradient Oswald type, ≤2 framed phones per page w/ varied portrayal), realistic PhoneFrame + BrowserFrame + ~20 unique animated app-screen mocks, REAL 3D anatomy viewer exported to public/anatomy/viewer.html (apps/mobile/scripts/anatomy/export_web_viewer.ts — rerun after anatomy:build-viewer) and embedded live on /training, full-screen animated red/black 404 (owner-spec template, mascot video), pricing live from tier_prices via loadPublicCatalog. Old landing/* components deleted (backup in session scratchpad). Gates: web tsc 0 errors. NOT committed.
- [x] 2026-07-19 deploy unblock + member-code verification (v1.1.2): Vercel prod deploys had been silently failing (hourly cron rejected on Hobby → tick now daily 03:00 UTC; upload >100MB from local .next/.turbo/.findex_db caches → .vercelignore extended), which had frozen the hosted API on a pre-notifications build — root cause of "push notifications/promos/everything not working" on device+web. Redeployed: all routes + CORS live. Member code now has a redemption surface: partner portal "Verify Member" (/partner/verify + POST /api/partner/verify-member — code→account lookup, first-name/tier/validity only, uniform not_found, 30/min rate limit) so restaurants can check a card before applying the member discount.
