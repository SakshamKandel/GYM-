import { SubscribeScreen } from '../features/subscription/SubscribeScreen';

/** /subscribe — the GM Method paywall. Pushed from upgrade prompts anywhere. */
export default function Subscribe() {
  return <SubscribeScreen />;
}
