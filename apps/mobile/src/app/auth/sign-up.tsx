import { AuthScreen } from '../../features/auth/AuthScreen';

/** /auth/sign-up — optional account creation (pushed from Settings). */
export default function SignUpScreen() {
  return <AuthScreen mode="signUp" />;
}
