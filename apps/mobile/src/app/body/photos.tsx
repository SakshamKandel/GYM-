import { ProgressPhotosScreen } from '../../features/body/progressPhotos/ProgressPhotosScreen';

/** Thin Expo Router entry; all behavior stays inside the Body feature. */
export default function ProgressPhotosRoute() {
  return <ProgressPhotosScreen />;
}
